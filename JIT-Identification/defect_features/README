
1. 需要安装的软件以及安装包：

   软件：mysql, python3，git； python安装包：sqlalchemy simplejson, mysql-connector


 2. config.json是配置文件

data_root_path: 是存储所有需要提取特征的项目目录的父目录
例如：在/home/test/projects/下存储的所有的项目，那么该字段需要填这个目录

projects: 是需要研究的所有项目

git_log_path: 程序提取数据时，需要先利用git命令生成所有commit的修改log，
因此，这里需要填写git log的生成目录。
当执行git log命令时，程序会在该目录下为每个项目创建一个目录。
每个目录下，生成5个文件，分别

sql: 这里填写myql的配置项，目前程序仅支持mysql存储。
dbname是程序运行时，数据存储的数据库名称

extensions: 程序提取的feature有很多都是与文件相关的。
你可以在这里配置你关注的文件类型，比如你认为java和c++文件是比较容易出bug的，
可以配置extensions为 [".java", ".h", ".c", ".cpp", ".hpp"]
extensions配置为[]时，程序将考虑所有的文件。

3. 程序运行顺序：
先运行log_generation.py
然后运行main.py

运行结束后，在你所配置的数据库中将会有6张表格，
